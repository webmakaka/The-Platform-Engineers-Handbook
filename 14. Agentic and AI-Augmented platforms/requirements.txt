# Chapter 14: AI-Augmented Platforms
# Core dependencies (required)
prometheus-client>=0.20.0
pyyaml>=6.0
scikit-learn>=1.3.0

# Anthropic Claude API (optional – scripts run in mock mode without these)
langchain>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.0.10
anthropic>=0.25.0

# Vector store and embeddings (optional – needed for production RAG)
chromadb>=0.4.0
sentence-transformers>=2.2.0
tiktoken>=0.5.0

# Kubernetes integration (optional – needed for live cluster demos only)
kubernetes>=28.0.0
